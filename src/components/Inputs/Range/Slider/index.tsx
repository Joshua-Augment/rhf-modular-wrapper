import React from 'react'
import { IFormFrameInjector, InputWrapper, ISlider } from '../../../core'

const Slider = (props: ISlider) => {
  return (
    <InputWrapper {...props}>
      {(IWProps: IFormFrameInjector) => <></>}
    </InputWrapper>
  )
}

export default Slider